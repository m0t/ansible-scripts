hosts: 192.168.56.101
remote_user: root
vars:
    apprepo: https://github.com/m0t/flask-test.git
tasks:
    - name: install pkgs
    apt: pkg={{item}} state=installed
    with_item:
        - name: nginx
        - name: git
        - name: flask
    - name: create app user
    - name: get app
    - 
